# AI Assistant — Phase 0 Implementation Review (final)

**Scope reviewed:** Phase 0 only, against `docs/AI_ASSISTANT_FULL_CAPABILITY_PLAN.md` §15 (Phase 0) and its acceptance criteria.
**Cycle:** re-review after the P0-1 … P0-4 fixes from review #1.
**Verdict: PASS.** All four findings are resolved, each one is now guarded by a test that was empirically proven to fail when the fix is reverted. No production defect was found in either cycle. Phase 0 is signed off.

**Reviewed artifacts:** `supabase/migrations/20260813120000_ai_assistant_phase0_foundations.sql`, `lib/ai/commercial-policy.ts`, `lib/ai/conversations.ts`, `types/database.ts`, `tests/unit/ai/phase0-foundations.test.ts`, `tests/unit/ai/p4b-conversations.test.ts`, `tests/unit/ai/p45c-commercial.test.ts`, `tests/unit/integration/helpers/local-policy.ts`, `tests/unit/integration/p4a-ai-tools-rls.test.ts`, `tests/unit/integration/p46-ai-permissions-rls.test.ts`, `tests/unit/integration/p45c-ai-commercial-integration.test.ts`, `docs/plans/AI_ASSISTANT_FULL_CAPABILITY_PLAN.md`.

---

## Verification performed (cycle 2)

| Check | Result |
| --- | --- |
| `npx tsc --noEmit` | clean |
| `pnpm test` (unit, integration excluded) | 331 files, **2388 passed** |
| `pnpm test:integration` (local stack, migration applied) | 49 passed / 1 skipped, **416 passed / 3 skipped** |
| `pnpm lint` | **0 errors**, 25 pre-existing warnings (all in untouched P7/validation files) |
| `supabase migration list --local` | `20260813120000` applied |
| Live-schema inspection | `agent_messages.sequence` → `attidentity = 'a'` (ALWAYS), `attnotnull = t`; both policy quals carry their new terms; `plans.features` for `pro_ai` carries all nine `ai.*` keys as `true` |

**Negative controls (the point of this cycle).** The pre-fix schema was restored on the local dev database in a single transaction — the `20260718160000` `agent_owner_all_messages` policy, the `using (user_id = auth.uid())` self-read policy, `alter column sequence set generated by default`, and `features - 'ai.write_privileged'` on `pro_ai` — and the three affected integration files were re-run:

```
× H1 — keeps role and patient-liveness checks on the message policy itself
    expected '((clinic_id = auth_clinic_id()) and (…' to contain 'auth_role() = any'
× M1 — keeps self-read scoped to the authenticated clinic in the policy itself
    expected '(user_id = auth.uid())' to contain 'clinic_id = auth_clinic_id()'
× rejects an authenticated owner supplying an arbitrary replay sequence
    expected undefined to be '428C9'
× seeds every Phase 0 AI capability key as enabled on Pro + AI
    expected { sms: true, 'ai.byok': true, …(22) } to match object { 'ai.read_operational': true, …(8) }

Tests  4 failed | 51 passed (55)
```

Exactly the four intended guards failed and nothing else did — each fix is now discriminating, and no other test silently depends on the fixed schema. The database was then restored from the migration's own DDL and the full integration suite re-run green (416 passed / 3 skipped), so the local stack is back to its post-migration state.

**H2 DDL order re-verified for the `always` change (P0-3).** The backfill → `set not null` + `add generated always as identity` → `setval` → unique-index sequence was executed against a throwaway schema and rolled back: the backfill lands 1..3 per conversation, `setval` yields 4, the next insert takes 4 without colliding on `(conversation_id, sequence)`, and a client-supplied `sequence` raises `generated_always`. The switch from `by default` to `always` does not disturb the migration's ordering constraints.

---

## Finding-by-finding closure

### P0-1 — H1 `agent_messages` policy is now guarded ✅

`tests/unit/integration/p4a-ai-tools-rls.test.ts:492-508` adds *"H1 — keeps role and patient-liveness checks on the message policy itself"*, which reads the installed `pg_policies.qual` through the new `tests/unit/integration/helpers/local-policy.ts` and asserts the policy independently carries `auth_role() = any`, all five `'<role>'::user_role` literals, `p.is_deleted = false` and `p.deleted_at is null`.

- ✔ Reverting the policy to its `20260718160000` form fails it (shown above).
- ✔ Every assertion names H1 in its message, so the reason for the redundancy is documented in the test.
- ✔ The pre-existing behavioural test survives, correctly relabelled *"keeps patient-liveness behavior in parity with the parent conversation policy"* (`:697`) so it can no longer be mistaken for the H1 guard. The rest of the file passes unchanged.

### P0-2 — M1 `user_ai_permissions` policy is now guarded ✅

`tests/unit/integration/p46-ai-permissions-rls.test.ts:246-256` asserts the installed qual contains both `user_id = auth.uid()` and `clinic_id = auth_clinic_id()`.

- ✔ Restoring `using (user_id = auth.uid())` fails it.
- ✔ Structural approach (option b) chosen, so no cross-clinic fixture row is created and no cleanup is needed.
- ✔ *"does not let a primary admin reach into another clinic"* and the service-role workflow test are unaffected.

### P0-3 — `sequence` is now `generated always as identity` ✅

Migration line 119 reads `alter column sequence add generated always as identity`; the live column reports `attidentity = 'a'`.

- ✔ `tests/unit/integration/p4a-ai-tools-rls.test.ts:751-780` inserts `sequence: 0` as the authenticated owner and asserts Postgres rejects it with `428C9` / *cannot insert a non-DEFAULT value into column "sequence"*.
- ✔ The 6-message replay test still passes (its service-role insert does not name `sequence`).
- ✔ `types/database.ts:157,168` now type Insert/Update as `sequence?: never`, so the class of write is unrepresentable in TS as well; `npx tsc --noEmit` is clean and `persistDoctorTurn` never supplies the column.

### P0-4 — the seed is asserted at the database, not as file text ✅

`tests/unit/integration/p45c-ai-commercial-integration.test.ts:226-238` reads `plans.features` for `slug = 'pro_ai'` and asserts every member of the new `AI_CAPABILITY_FEATURES` export resolves to `true`, pinning the list length at 9.

- ✔ Deleting `ai.write_privileged` from the row fails it.
- ✔ The H2/seed SQL-substring assertions are gone from `tests/unit/ai/phase0-foundations.test.ts`; only the plan-copy check and the legitimate Phase-0b non-crossing text guard remain (unit count moved 638 → 636 accordingly).
- ✔ `AI_CAPABILITY_FEATURES` is spread into `NAMESPACED_AI_FEATURES`, and `p45c-commercial.test.ts:27-54` now pins the full 21-key vocabulary by exhaustive equality plus a duplicate check and a per-key `isKnownAiFeature()` assertion — still the TS-side authority, not duplicated by the DB test.

---

## Scope discipline (verified clean, cycle 2)

- **No Phase 0b or later leakage.** The migration contains no `effective_ai_feature` / `resolve_ai_commercial_limits` rewrite, no `accepted_at` predicate; `lib/entitlements.ts`, `actions/operator.ts`, `lib/ai/permissions.ts` and `actions/ai-permissions.ts` are all clean in `git status`; `lib/ai/resources/**` and `lib/ai/actions/**` do not exist; no RAG work (Decision 8 respected).
- **No unrelated dirty work touched.** Every file modified since 2026-08-12 is a Phase 0 file, and the set is exactly: `lib/ai/commercial-policy.ts`, `lib/ai/conversations.ts`, `types/database.ts`, the migration, `tests/unit/ai/phase0-foundations.test.ts`, `tests/unit/ai/p4b-conversations.test.ts`, `tests/unit/ai/p45c-commercial.test.ts`, `tests/unit/integration/p4a-ai-tools-rls.test.ts`, `tests/unit/integration/p46-ai-permissions-rls.test.ts`, `tests/unit/integration/p45c-ai-commercial-integration.test.ts`, `tests/unit/integration/helpers/local-policy.ts`, `docs/AI_ASSISTANT_FULL_CAPABILITY_PLAN.md` (+ its `docs/plans/` copy) and this report. The other ~41 dirty files are the pre-existing P7 document work (mtimes 2026-08-09) — `lib/supabase/admin.ts` was re-checked specifically and its only diff (`findCompletedClinicDocument`) is P7, dated 08-09.
- **§15 deliverables all met.** Plan copied to `docs/plans/` and byte-identical (asserted by test); H1/H2/M1 fixed in migration; nine keys added to `NAMESPACED_AI_FEATURES` and seeded on `pro_ai`; RLS-isolation, deterministic-replay and catalog-completeness tests present. All three acceptance lines hold: an admin cannot read a colleague's conversation (`p4a-ai-tools-rls.test.ts:686`), a 6-message conversation replays stably (`:631` at the DB, `p4b-conversations.test.ts:63` at the client), every AI feature key is `isKnownAiFeature()`.

## Notes carried forward (not findings)

- `tests/unit/integration/helpers/local-policy.ts` shells out to `docker exec … psql` rather than going through the Supabase JS client. This is the right call for reading `pg_policies` (PostgREST cannot), and the integration suite already requires the local stack, but it is the first test helper to depend on the Docker CLI and the container name derived from `supabase/config.toml`. Worth remembering if the integration suite is ever moved to a hosted branch database.
- The nine seeded keys remain inert — nothing resolves or consumes them until Phase 0b re-points the resolver. That is the intended Phase 0 end state.
- Phase 0b's plan text lists `p45c-ai-commercial-integration.test.ts:226-241` as a test to rewrite; Phase 0 inserted the new seed test at exactly line 226, so the coupling assertions it refers to now start at `:240`. Adjust the line reference when Phase 0b starts.
